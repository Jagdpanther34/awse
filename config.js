// ───────────────────────────────────────────────────────────────
//  接続先サーバーの設定
//
//  サーバーの持ち主からもらった「URL」をここに入れてデプロイします。
//  ※ APIキーはここに書きません（コードに書くと公開時に誰でも見られるため）。
//     キーは利用者が画面の「API Key」欄に入力し、各自のブラウザに保存されます。
// ───────────────────────────────────────────────────────────────

export const DEFAULT_SERVER = {
  name: "My LLM Server",                 // 画面に表示される名前（任意）
  type: "openai",                        // OpenAI互換API
  baseUrl: "https://YOUR-SERVER-HERE/v1", // ← もらったURLに書き換える（末尾は通常 /v1）

  // 利用者にキー入力を必須とするか。
  //  true  : キーが未入力なら入力を促すバナーを表示（推奨）
  //  false : キー不要なサーバーの場合
  requireApiKey: true,
};
